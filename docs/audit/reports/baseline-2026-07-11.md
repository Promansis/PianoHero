# Baseline Report: 2026-07-11

## Identity

- Source branch/worktree: `docs/audit-workflow` at `/media/storage/PianoHero`
- Commit SHA: `808e6739e55b5186e8a9565f2c4e9267c4894a6c`
- Dirty patch identity: tracked working-tree diff SHA-256 `1bc69ee417613bde56d64630e7cfd4b30ad976d3b80d363a166fb8bcef9c57ae`, captured before this report and the Phase 1 matrix update. Non-audit untracked source: `CONTEXT.md`, `docs/adr/*`, `docs/agents/*`, `scripts/ensure-native-module.cjs`, and `src/renderer/components/DeckBackdrop.tsx`.
- OS/architecture: Linux 6.8.0-134-generic x86_64
- Node/npm versions: Node v24.15.0; npm 11.12.1
- Electron version: 30.5.1 smoke-tested
- Browser versions: Google Chrome 149.0.7827.114 smoke-tested. Supported browser policy: current stable desktop Chrome, Edge, and Firefox.
- MIDI/audio hardware: not tested
- Isolated data directories: web `/tmp/pianohero-audit-20260711-phase0`; Electron `/tmp/pianohero-audit-electron-{config,data,cache}`. No production or personal data was used.

## Dependency Health

- Initial condition: package entrypoints were non-executable and both esbuild binary links targeted missing `/tmp/esbuild`, so `npm run typecheck` and `npm test` exited 127.
- Repair: `npm ci` completed without a `package-lock.json` diff. It restored executable TypeScript, Vitest, Vite, and esbuild binaries. `better-sqlite3` requires different ABIs for Node 24 and Electron 30, so the repository now checks the active runtime and rebuilds only when needed.
- Current native checks: TypeScript, Vitest, Vite, and esbuild entrypoints are executable (`0755`/`0775`); Node and Electron native probes both passed after their respective guard paths.
- Files changed by repair: ignored `node_modules`; tracked repeatability guard in `package.json`, `scripts/postinstall.cjs`, and `scripts/ensure-native-module.cjs`. The lockfile was unchanged.

## Automated Baseline

| Check | Exact command | Result | Duration | Evidence/summary |
|---|---|---|---:|---|
| Typecheck | `npm run typecheck` | pass | under 10s | Strict TypeScript completed with no diagnostics. |
| Tests | `PIANOHERO_DATA_DIR=/tmp/pianohero-audit-20260711-phase0 npm test` | pass | 8s | 57 test files and 285 tests passed; the pretest guard rebuilt `better-sqlite3` for Node automatically. |
| Desktop build | `npm run build` | pass | under 10s | Electron main, preload, and renderer built. Vite left two main-menu image references for runtime resolution. |
| Web build | `PIANOHERO_DATA_DIR=/tmp/pianohero-audit-20260711-phase0 npm run build:web` | pass | under 10s | Server and web client built. Vite warned about chunks larger than 500 kB after minification. |
| Package audit | `npm audit` | fail, exit 1 | under 10s | 48 advisories: 3 critical, 23 high, 14 moderate, 8 low. This remains a Phase 2 supply-chain/exposure lead, not a product finding. |

## Startup Smoke

### Electron

- Exact command/profile: after `node scripts/ensure-native-module.cjs electron`, launched `./node_modules/.bin/electron --no-sandbox .` under `xvfb-run -a` with the isolated XDG directories above. `--no-sandbox` was used only because the CI-style Xvfb environment cannot satisfy Electron's SUID sandbox requirement; it is not a product or packaging setting.
- Window rendered: yes. `xwininfo` recorded `LumaKeys` at `1480x960`.
- Main menu interaction: not exercised in Electron's first-run profile; the equivalent isolated web smoke completed setup, main menu, and library navigation. Electron workflow interaction remains a Phase 4/5 evidence item.
- Clean shutdown: yes. A disposable harness sent SIGTERM to Electron and exited 0 before Xvfb teardown.
- Console/main-process errors: no application exception. Headless GPU/DBus warnings are environmental; ALSA MIDI-sequencer access is unavailable in the container.
- Evidence: Xvfb process/window tree and isolated runtime lifecycle recorded during this baseline. The web screenshot hash is `c2b27ae22094318fa2a4cb6e4de48fa1ffec33d063b380d65cc717c9f4df86f6` at `/tmp/pianohero-audit-web-smoke-20260711.png`.

### Web

- Exact server/client command: `PIANOHERO_DATA_DIR=/tmp/pianohero-audit-20260711-phase0 PORT=3101 npm run start:web`; Chrome opened `http://127.0.0.1:3101/`.
- `PIANOHERO_DATA_DIR`: `/tmp/pianohero-audit-20260711-phase0`
- URL and browser: `http://127.0.0.1:3101/`, Google Chrome 149.0.7827.114
- Main menu interaction: pass. First-run setup rendered, `Skip for now` reached the main menu, and `Open Library` navigated into the empty library.
- Clean shutdown: pass. The server received SIGTERM and port 3101 was released.
- Browser/server errors: zero browser console errors. One warning reported the preloaded main-menu background was not used immediately.
- Evidence: `/api/access` and `/` returned 200; bridge setting and achievement calls returned 200 during the UI smoke.

## Known Baseline Failures

| ID | Failure | Environmental or product lead | Reproduction | Audit effect | Owner |
|---|---|---|---|---|---|
| BASE-ENV-001 | Broken local package launchers and esbuild links before repair | Environmental, resolved | Former `npm run typecheck`/`npm test` exit 127; repaired with `npm ci` and guarded native rebuilds | Historical only; no product finding | Codex (acting) |
| BASE-SEC-001 | `npm audit` reports 48 dependency advisories | Security lead | `npm audit` exits 1 | Requires Phase 2 supply-chain and exposure analysis | Security lane owner |
| BASE-COND-001 | Windows, Edge, Firefox, high-DPI, and MIDI-hardware proof has not yet been collected | Audit execution gap | Charter supported-condition table and Phase 1 plan | Does not block evidence collection; limits current baseline claims to Linux/Chrome | Codex (acting) |

## Baseline Decision

- Suitable for audit evidence: yes, within the confirmed desktop scope.
- Exceptions and confidence impact: only Linux/Chrome has runtime smoke evidence so far. Electron first-run menu interaction, Windows, Edge, Firefox, high-DPI, and hardware MIDI/audio remain later-lane work. Do not generalize the current smoke result to those conditions.
- Approved by: repository owner confirmed `1180x780`, `1920x1080`, Linux/Windows x64, and current stable Chrome/Edge/Firefox on 2026-07-11
- Date: 2026-07-11
